export { default as SideBar } from "./Sider";
export { default as FirstPage } from "./FirstPage";

// const components: any = {};
// require
//   .context("./", true, /.\/(.*)\/$/)
//   .keys()
//   .map((str) => str.replace(/(\/)|(\.)/g, ""))
//   .map((item: string) => (components[item] = require(`./${[item]}`).default));

// export default components;
